# Examples

## Example usage of the `terraform-dotenvx` module

| Folder | Description |
|---|---|
| [Simple](./simple/) | Simple example using dotenvx and terraform together.|
| [Vercel](./vercel/) | Using dotenvx and terraform to create a vercel project, with `preview`, `development` and `production` vercel environment variables managed by dotenvx `.env.preview` , `.env.development` , and `.env.production` files respectively.|
